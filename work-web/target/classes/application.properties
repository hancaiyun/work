#
#
# Datasource settings
spring.datasource.driver-class-name=${spring.datasource.driver-class-name}
spring.datasource.url=${spring.datasource.url}
spring.datasource.username=${spring.datasource.username}
spring.datasource.password=${spring.datasource.password}
# Hikari
spring.datasource.type=${spring.datasource.type}
spring.datasource.hikari.minimum-idle=${spring.datasource.hikari.minimum-idle}
spring.datasource.hikari.maximum-pool-size=${spring.datasource.hikari.maximum-pool-size}
spring.datasource.hikari.auto-commit=${spring.datasource.hikari.auto-commit}
spring.datasource.hikari.idle-timeout=${spring.datasource.hikari.idle-timeout}
spring.datasource.hikari.pool-name=${spring.datasource.hikari.pool-name}
spring.datasource.hikari.max-lifetime=${spring.datasource.hikari.max-lifetime}
spring.datasource.hikari.connection-timeout=${spring.datasource.hikari.connection-timeout}
spring.datasource.hikari.connection-test-query=${spring.datasource.hikari.connection-test-query}
#
#
logger_logback_logpath=${logger_logback_logpath}
#
# Mybatis settings
mybatis.mapper-locations=${mybatis.mapper-locations}
mybatis.type-aliases-package=${mybatis.type-aliases-package}
#
#

# Dubbo settings
#dubbo.application.name=${dubbo.application.name}
#dubbo.protocol.name=${dubbo.protocol.name}
#dubbo.protocol.port=${dubbo.protocol.port}
#dubbo.protocol.threads=${dubbo.protocol.threads}
#dubbo.registry.protocol=${dubbo.registry.protocol}
#dubbo.registry.address=${dubbo.registry.address}
#
#
#KAFKA
spring.kafka.producer.acks=${spring.kafka.producer.acks}
spring.kafka.producer.retries=${spring.kafka.producer.retries}
spring.kafka.producer.bootstrap-servers=${spring.kafka.producer.bootstrap-servers}
spring.kafka.producer.topicName=${spring.kafka.producer.topicName}
spring.kafka.producer.client-id=${spring.kafka.producer.client-id}

#consumer
spring.kafka.consumer.group-id=${spring.kafka.consumer.group-id}
spring.kafka.consumer.bootstrap-servers=${spring.kafka.consumer.bootstrap-servers}
spring.kafka.consumer.enable-auto-commit=${spring.kafka.consumer.enable-auto-commit}
spring.kafka.max.poll.records = ${spring.kafka.max.poll.records}
spring.kafka.consumer.concurrency =${spring.kafka.consumer.concurrency}
#n
#s
spring.redis.database=${spring.redis.database}
spring.redis.sentinel.master=${spring.redis.sentinel.master}
spring.redis.sentinel.nodes=${spring.redis.sentinel.nodes}
spring.redis.pool.max-active=${spring.redis.pool.max-active}
spring.redis.pool.max-wait=${spring.redis.pool.max-wait}
spring.redis.pool.max-idle=${spring.redis.pool.max-idle}
spring.redis.pool.min-idle=${spring.redis.pool.min-idle}
spring.redis.timeout=${spring.redis.timeout}